# Example environment variables for the Next.js frontend.
# Copy this file to `.env.local` and set appropriate values for your
# deployment.  In a Docker compose environment, these may be
# overridden via environment variables in the compose file.

# Base URL of the FastAPI backend.  When running within Docker
# compose, `http://app:8000` points to the `app` service on the
# internal network.  For local development outside Docker, you might
# use `http://localhost:8000` instead.
BACKEND_URL=http://app:8000