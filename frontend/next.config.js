/**
 * @type {import('next').NextConfig}
 *
 * Next.js configuration for the chat frontend.  The App Router is
 * enabled by default in Next.js 13+, so we simply enable strict
 * mode.  Additional configuration (such as environment variables or
 * image domains) can be added here as needed.
 */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;